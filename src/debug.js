// imports
const cardCrawler = require('./crawler/card');

// Temple Garden
const cardId = 89093;

cardCrawler.get(cardId).then((card) => {
    console.log(error);
    }, (error) => console.log(error)
);
